[project]
name = "ai-course"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "mlflow==3.16.0",
    "mlflow-skinny>=3.16.0",
    "pandas-stubs~=3.0.5",
    "xgboost>=3.4.1",
]

[tool.uv.workspace]
members = [
    "churn-baseline",
]
